#!/bin/sh

echo "🔍 Running pre-commit checks..."

# Run lint-staged
pnpm lint-staged

if [ $? -ne 0 ]; then
  echo "❌ Linting and formatting failed"
  exit 1
fi

echo "✅ Pre-commit checks passed!"
